# Claude Code Adapter

Use `installers/install.py --target claude-code --dest <plugin-dir>` to create a Claude Code-compatible plugin layout with `commands/`, `skills/`, and `.claude-plugin/plugin.json`.

Claude Code plugin commands are markdown files with description frontmatter. Skills live under `skills/<skill-name>/SKILL.md`.
