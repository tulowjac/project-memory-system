# Project Memory System — Antigravity Adapter

This adapter is intentionally portable. Read `skills/project-memory-system/SKILL.md` and use the command markdown files in `commands/` as slash-command style workflows.

If Antigravity exposes a stable native plugin manifest, add it as a thin adapter while keeping `core/` canonical.
